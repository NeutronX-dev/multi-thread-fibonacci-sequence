#ifndef MAIN_H_DEF
    #define MAIN_H_DEF
    #define PRINT(x) std::cout << x << std::endl
    // Default
    #include <iostream>

    // Data-types
    #include <vector>
    #include <string>

    // Utilities
    #include <thread>
    #include <chrono>
#endif
